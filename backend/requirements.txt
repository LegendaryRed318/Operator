# Operator Backend Dependencies
# Install with: pip install -r requirements.txt

# Core API (choose one)
# For FastAPI (recommended):
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.5  # Required for file uploads

# For legacy server (optional - kept for compatibility):
# (no extra deps needed, uses stdlib)

# WebSocket server
websockets>=16.0

# AI/ML
faster-whisper>=0.10.0
ollama>=0.1.0

# HTTP client
aiohttp>=3.8.0
aiofiles>=23.0.0
requests>=2.31.0

# System monitoring
psutil>=5.9.0
pyttsx3>=2.90
WMI>=1.5.1; platform_system == "Windows"

# Database
# (sqlite3 is built-in)

# File watching
watchdog>=3.0.0

# Obsidian vault integration
chromadb>=0.4.0
sentence-transformers>=2.2.0
python-dotenv>=1.0.0

# Voice Service (Whisper VAD)
sounddevice>=0.4.6
silero-vad>=5.1
openai-whisper>=20231117
scipy>=1.10.0
numpy>=1.24.0

# Image processing (for screenshots)
Pillow>=10.0.0

# TOML parsing (for skills)
toml>=0.10.2
